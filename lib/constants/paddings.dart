import 'package:flutter/material.dart';

// horizontal
const horizontal20 = EdgeInsets.symmetric(horizontal: 20);
const all5 = EdgeInsets.all(5);
const all25 = EdgeInsets.all(25);
const all10 = EdgeInsets.all(10);
const all16 = EdgeInsets.all(16);
const only12Bot = EdgeInsets.only(bottom: 12);