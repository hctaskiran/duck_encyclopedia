import 'package:flutter/material.dart';

class InspirationDialog extends StatelessWidget {
  const InspirationDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const AlertDialog(
      title: Text('Alofısfıs'),
    );
  }
}